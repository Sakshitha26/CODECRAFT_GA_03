# CODECRAFT_GA_03
A Python-based generative model that uses the Markov Property $P(X_{n+1} | X_n)$ to predict and synthesize text. This project explores statistical NLP by building a frequency dictionary of word transitions, allowing it to generate unique, contextually-linked sentences from user-provided data.
